name: WordDoc
description: ocr for word documents is done here
inputs:
- {name: Input_data, type: GCSpath}
outputs:
- {name: Output_data, type: GCSpath}
implementation:
  container:
    image: gcr.io/mlops-kubeflow-00/word_doc:latest
    command:
    args: [
      --input_data, {inputPath: input_data},
      --output_data, {outputPath: output_data}
    ]